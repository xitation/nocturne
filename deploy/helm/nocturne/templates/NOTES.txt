Nocturne {{ default .Chart.AppVersion (default .Values.api.image.tag .Values.web.image.tag) }} has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

{{- if .Values.bootstrap.enabled }}

The pre-install bootstrap Job created/refreshed three PostgreSQL roles
(nocturne_migrator, nocturne_app, nocturne_web) on:
  {{ .Values.externalDatabase.host }}:{{ .Values.externalDatabase.port }} / {{ .Values.externalDatabase.database }}

To re-run the bootstrap manually:
  kubectl -n {{ .Release.Namespace }} delete job {{ include "nocturne.bootstrap.fullname" . }} --ignore-not-found
  helm upgrade {{ .Release.Name }} <chart>
{{- else }}

Bootstrap Job is DISABLED. The chart assumes the three Postgres roles
(nocturne_migrator, nocturne_app, nocturne_web) already exist on:
  {{ .Values.externalDatabase.host }}:{{ .Values.externalDatabase.port }} / {{ .Values.externalDatabase.database }}

If they do not, see:
  https://github.com/nightscout/nocturne/blob/main/docs/postgres/bootstrap-roles.sql
{{- end }}

{{- if .Values.ingress.enabled }}

Nocturne should be reachable at:
  {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}/

{{- if .Values.ingress.api.externalPath }}
The API is also exposed at:
  {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}{{ .Values.ingress.api.externalPath }}
{{- end }}
{{- else }}

Ingress is disabled. To reach the web UI from your workstation:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "nocturne.web.fullname" . }} 5173:{{ .Values.web.service.port }}
  open http://localhost:5173
{{- end }}

To check pod status:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

To view API logs:
  kubectl -n {{ .Release.Namespace }} logs -f deploy/{{ include "nocturne.api.fullname" . }}
