// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';

/** Approve or deny a device authorization request.
Called by the device approval page. */
export const deviceApprove = command(z.object({ user_code: z.string().optional(), approved: z.boolean().optional() }), async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const body = new URLSearchParams();
    body.set('user_code', String(request.user_code));
    body.set('approved', String(request.approved));
    const event = getRequestEvent();
    const headers: Record<string, string> = { 'Content-Type': 'application/x-www-form-urlencoded' };
    const host = event.request.headers.get('host');
    if (host) headers['X-Forwarded-Host'] = host;
    const cookie = event.request.headers.get('cookie');
    if (cookie) headers['Cookie'] = cookie;
    const response = await event.fetch(apiClient.baseUrl + '/api/oauth/device-approve', {
      method: 'POST',
      headers,
      body: body.toString(),
    });
    if (!response.ok) {
      const err: any = new Error(`Request failed (${response.status})`);
      err.status = response.status;
      throw err;
    }
    
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oAuth.deviceApprove:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([k, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to device approve');
  }
});
