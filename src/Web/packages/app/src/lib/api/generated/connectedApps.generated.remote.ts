// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';

/** List all connected apps (OAuth app grants) for the authenticated user
on the current tenant. */
export const list = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.connectedApps.list();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in connectedApps.list:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to list');
  }
});

/** Revoke a connected app. Soft-deletes the grant and invalidates all
associated refresh tokens; previously-issued access tokens become
unusable on next request via the revocation cache. */
export const revoke = command(z.string(), async (grantId) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.connectedApps.revoke(grantId);
    await Promise.all([
      list(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in connectedApps.revoke:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to revoke');
  }
});
