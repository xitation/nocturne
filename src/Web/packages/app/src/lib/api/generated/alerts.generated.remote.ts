// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { AcknowledgeRequestSchema, SnoozeRequestSchema } from '$lib/api/generated/schemas';
import { ChannelType, type AcknowledgeRequest, type SnoozeRequest } from '$api';

/** List active (unresolved) excursions for the current tenant. */
export const getActiveAlerts = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.alerts.getActiveAlerts();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in alerts.getActiveAlerts:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get active alerts');
  }
});

/** Get paginated history of resolved excursions. Test fires are excluded
by default; pass includeTest = true to include them. */
export const getAlertHistory = query(z.object({ page: z.number().optional(), pageSize: z.number().optional(), alertRuleId: z.string().optional(), includeTest: z.boolean().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.alerts.getAlertHistory(params?.page, params?.pageSize, params?.alertRuleId, params?.includeTest);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in alerts.getAlertHistory:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get alert history');
  }
});

export const acknowledge = command(AcknowledgeRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.alerts.acknowledge(request as AcknowledgeRequest);
    await Promise.all([
      getActiveAlerts(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in alerts.acknowledge:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to acknowledge');
  }
});

/** Snooze an alert instance for the specified duration. */
export const snoozeInstance = command(z.object({ instanceId: z.string(), request: SnoozeRequestSchema }), async ({ instanceId, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.alerts.snoozeInstance(instanceId, request as SnoozeRequest);
    await Promise.all([
      getActiveAlerts(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in alerts.snoozeInstance:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to snooze instance');
  }
});

/** Get pending deliveries for the specified channel types.
Used by bot/adapter services to poll for work. */
export const getPendingDeliveries = query(z.object({ channelType: z.array(z.nativeEnum(ChannelType)).optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.alerts.getPendingDeliveries(params?.channelType);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in alerts.getPendingDeliveries:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get pending deliveries');
  }
});
