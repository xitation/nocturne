// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { AlertReplayRequestSchema, AlertReplayDryRunRequestSchema } from '$lib/api/generated/schemas';
import { type AlertReplayRequest, type AlertReplayDryRunRequest } from '$api';

/** Replay enabled rules over a window. date=null replays the rolling last 24 hours;
otherwise replays that calendar day in timezone (UTC if omitted). */
export const replay = command(AlertReplayRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.alertReplay.replay(request as AlertReplayRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in alertReplay.replay:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([k, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to replay');
  }
});

/** Dry-run replay variant for the rule editor. Layers a user-provided rule definition
onto the saved rule set for one call (never persisted), so authors can answer
"would my new/edited rule have woken me last night?" before saving. */
export const replayDryRun = command(AlertReplayDryRunRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.alertReplay.replayDryRun(request as AlertReplayDryRunRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in alertReplay.replayDryRun:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([k, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to replay dry run');
  }
});
