// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { CreateOidcProviderRequestSchema, UpdateOidcProviderRequestSchema, TestProviderRequestSchema } from '$lib/api/generated/schemas';
import { type CreateOidcProviderRequest, type UpdateOidcProviderRequest, type TestProviderRequest } from '$api';

export const getAll = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.oidcProviderAdmin.getAll();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.getAll:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get all');
  }
});

export const create = command(CreateOidcProviderRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.oidcProviderAdmin.create(request as CreateOidcProviderRequest);
    await Promise.all([
      getAll(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.create:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to create');
  }
});

export const getConfigManaged = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.oidcProviderAdmin.getConfigManaged();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.getConfigManaged:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get config managed');
  }
});

export const getById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.oidcProviderAdmin.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.getById:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to get by id');
  }
});

export const update = command(z.object({ id: z.string(), request: UpdateOidcProviderRequestSchema }), async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.oidcProviderAdmin.update(id, request as UpdateOidcProviderRequest);
    await Promise.all([
      getAll(undefined).refresh(),
      getById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.update:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to update');
  }
});

export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.oidcProviderAdmin.delete(id);
    await Promise.all([
      getAll(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.delete:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to remove');
  }
});

export const enable = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.oidcProviderAdmin.enable(id);
    await Promise.all([
      getAll(undefined).refresh(),
      getById(id).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.enable:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to enable');
  }
});

export const disable = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.oidcProviderAdmin.disable(id);
    await Promise.all([
      getAll(undefined).refresh(),
      getById(id).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.disable:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to disable');
  }
});

export const testExisting = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.oidcProviderAdmin.testExisting(id);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.testExisting:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to test existing');
  }
});

export const testUnsaved = command(TestProviderRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.oidcProviderAdmin.testUnsaved(request as TestProviderRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in oidcProviderAdmin.testUnsaved:', err);
    const body = (err as any)?.body ?? (err as any)?.response;
    const message = body?.message ?? body?.title ?? body?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, 'Failed to test unsaved');
  }
});
